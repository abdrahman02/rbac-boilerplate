import { RegisterForm } from '@/features/auth/components/RegisterForm'

export const metadata = {
  title: 'Register - RBAC Boilerplate',
}

export default function RegisterPage() {
  return <RegisterForm />
}
